<Page x:Class="Database.View.Pages.Query"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Database.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Query" Background="Wheat">

    <Page.Resources>
        <Style x:Key="Btn" TargetType="Button">
            <Setter Property="Margin" Value="10,0,10,10"></Setter>
        </Style>
    </Page.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="22*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>
        <DataGrid Grid.ColumnSpan="4" Margin="10"></DataGrid>
        <Button Grid.Row="1" Grid.Column="0" Style="{Binding Source={StaticResource Btn}}"></Button>
        <Button Grid.Row="1" Grid.Column="1" Style="{Binding Source={StaticResource Btn}}"></Button>
        <Button Grid.Row="1" Grid.Column="2" Style="{Binding Source={StaticResource Btn}}"></Button>
        <Button Grid.Row="1" Grid.Column="3" Style="{Binding Source={StaticResource Btn}}"></Button>
    </Grid>
</Page>
